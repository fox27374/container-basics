# Configuration for mirror-images.sh
#
# Edit this file to change the target registry or the list of images.
# It is sourced by the script, so standard shell syntax applies.

# Target registry (host[:port]). Can also be overridden with the REGISTRY
# environment variable when running the script.
REGISTRY="cr.lab.local"

# Upstream images to mirror. One entry per line, inside the parentheses.
IMAGES=(
    "docker.io/nginx:latest"
    "docker.io/nginx:alpine"
    "docker.io/alpine:latest"
    "docker.io/alpine:3.24.1"
    "docker.io/mysql:8.0"
    "docker.io/wordpress:7.0.0-php8.3-apache"
    "docker.io/splunk/splunk:latest"
)
